# Project context

@AGENTS.md

<!--
One canonical context file, two filenames. Claude Code reads CLAUDE.md, Codex
reads AGENTS.md — so this file imports the real one instead of duplicating it.

The previous repo kept two full copies (CLAUDE.md and agents/AGENTS.md). They
drifted: the copy lost the monitoring-GUI section, eight environment variables,
and still described the watchdog behaviour that had already been changed. Do not
reintroduce a second copy.
-->
